# 77-01 Plan

1. Add a reusable `model_failure_lab.analysis` report contract with structured report, pattern,
   anomaly, and evidence reference models.
2. Implement deterministic heuristic summarization over case and delta query rows using existing
   index/reporting concepts.
3. Carry bounded sampling metadata and explicit evidence references in every generated report.

