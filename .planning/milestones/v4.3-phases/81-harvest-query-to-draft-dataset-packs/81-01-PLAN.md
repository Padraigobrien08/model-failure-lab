# 81-01 Plan

1. Extend the canonical dataset envelope with the minimal top-level harvest fields needed for
   draft packs: `created_at`, `lifecycle`, and `source`.
2. Implement one harvest pipeline over the query layer that rehydrates canonical prompt content
   from saved run artifacts and writes draft dataset JSON packs.
3. Keep draft case ids deterministic and preserve explicit per-case provenance under
   `metadata.harvest`.
