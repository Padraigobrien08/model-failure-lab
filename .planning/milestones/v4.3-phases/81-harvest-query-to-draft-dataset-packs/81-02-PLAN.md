# 81-02 Plan

1. Add the `failure-lab harvest` CLI surface with query-compatible filters plus a comparison-first
   `--comparison` alias.
2. Add deterministic fixture-backed tests for both run-case harvesting and comparison-delta
   harvesting.
3. Keep the Phase 81 output strictly draft-scoped; no review, promotion, or UI export yet.
