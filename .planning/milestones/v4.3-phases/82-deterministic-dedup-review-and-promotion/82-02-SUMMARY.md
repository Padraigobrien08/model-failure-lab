# 82-02 Summary

- Added the `failure-lab dataset review` and `failure-lab dataset promote` CLI lifecycle.
- Extended `failure-lab datasets list` to show local dataset packs under the active artifact root
  while keeping the bundled dataset catalog intact.
- Verified that promoted harvested datasets appear in the catalog and run through the standard
  engine without manual reshaping.
