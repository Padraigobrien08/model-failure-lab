# 83-01 Plan

- Extend the query bridge and frontend artifact loader layer with a dedicated harvest endpoint.
- Add `/analysis` export using the current URL-backed filters and a deterministic output stem.
- Reset stale export state when the active query changes.
