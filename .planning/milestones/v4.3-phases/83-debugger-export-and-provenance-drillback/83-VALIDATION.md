# 83 Validation

## Must-Pass Stories

1. Analysis export:
   a filtered `/analysis` result set can be exported as a draft dataset pack and the user sees the
   written dataset id, path, and selected case count.
2. Comparison export:
   the selected comparison transition case can export its transition slice as a draft dataset pack
   from the focused evidence panel.
3. Shared debugger safety:
   route loaders, detail routes, and build output remain clean after adding the harvest bridge.
