# Phase 84 Research

- The deterministic insight fixture already provides multi-run comparisons and a rebuilt query
  index, so it is the right root for replay-loop verification.
- Phase 82 already proved review, promotion, and local dataset catalog discovery individually; the
  remaining bar is chaining those steps through fresh reruns and insight surfaces.
- The CLI already supports explicit adapter routing, so the fixture adapter/classifier can power
  reruns without adding another testing-only runner path.
