# 86-01 Plan

- Add `--score`, `--summary`, `--alert`, and `--alert-threshold` to `failure-lab compare`.
- Add a dedicated `failure-lab regressions` command over the mirrored comparison signal rows.
- Cover raw JSON, deterministic summary text, thresholded silence, and severity-ordered listings in
  focused CLI tests.
