# 87-01 Plan

1. Extend the query bridge, frontend loaders, and route types so comparison signals can flow into
   debugger state and `/analysis` signal mode.
2. Surface signal badges, severity, and top drivers in the comparison inventory and detail framing
   strip, with direct handoff into the existing transition evidence selection.
3. Add frontend regression coverage for severity ordering, comparison detail signal surfacing, and
   analysis signal mode, then verify the full debugger slice with a production build.
