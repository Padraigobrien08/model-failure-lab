# 89-01 Plan

1. Add a dedicated dataset-evolution module that can read saved comparison signals and select
   regression cases deterministically.
2. Rehydrate canonical prompt cases from saved run artifacts and write draft regression packs as
   standard dataset JSON.
3. Preserve explicit policy, signal, and evidence provenance in both the dataset envelope and the
   preview payload returned to callers.
