# 90-01 Plan

1. Add immutable version records and family-history scanning over canonical dataset artifacts.
2. Implement deterministic dataset evolution that composes new signal-selected cases with the latest
   immutable family version.
3. Persist parent-version lineage, source-comparison linkage, and stable duplicate-collapse results
   in version metadata.
