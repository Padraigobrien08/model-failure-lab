# 90-02 Plan

1. Expose dataset history and evolution through `failure-lab dataset versions` and
   `failure-lab dataset evolve`.
2. Keep CLI output machine-readable through JSON mode and concise human-readable summaries.
3. Prove that generated/evolved datasets still replay through the standard dataset -> run -> compare
   path.
