# 91 Research

- The analysis signal cards and comparison framing area are the right UI insertion points because
  they already answer “what changed?” before users open denser evidence.
- A shared component keeps family-id suggestion, action wiring, and history rendering consistent
  across routes.
- The route-level return-state helpers already solve provenance drillback; the new panel only needs
  to reuse them.
