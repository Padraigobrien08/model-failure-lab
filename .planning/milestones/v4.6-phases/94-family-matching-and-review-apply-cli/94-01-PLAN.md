# 94-01 Plan

1. Extend the governance backend with read-only family-health inspection and recent-comparison
   review helpers.
2. Add an apply helper that reuses immutable dataset evolution for both `create` and `evolve`
   actions in deterministic signal order.
3. Keep all outputs typed and JSON-friendly so the later debugger phase can reuse them.
