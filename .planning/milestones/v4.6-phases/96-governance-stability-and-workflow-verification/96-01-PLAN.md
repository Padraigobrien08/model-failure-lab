# 96-01 Plan

1. Add an integrated CLI governance workflow test that starts from a fresh `compare` artifact and
   proves recommend -> review -> apply -> family health.
2. Reuse the deterministic adapter/classifier fixture so the loop stays fast and reproducible.
3. Assert repeat apply behavior stays stable after the first dataset action is written.
