# 97 Validation

- Rebuilding the query index from local artifacts should surface ordered run, comparison, and
  dataset-family history without external state.
- Dataset-version lineage should remain reproducible across repeated index rebuilds.
- History snapshots must remain stable for dataset, model, and family scopes.
