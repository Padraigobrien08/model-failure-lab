# 102-01 Plan

1. Add `clusters`, `cluster show`, and `cluster history` CLI commands over the new cluster helpers.
2. Extend the query bridge and Vite middleware so `/analysis` can request cluster summaries from
   the same artifact-root contract.
3. Add CLI regression tests that prove JSON list/detail output plus readable cluster history.
