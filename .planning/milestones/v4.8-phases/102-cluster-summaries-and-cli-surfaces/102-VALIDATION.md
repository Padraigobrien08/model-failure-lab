# 102 Validation

- Users must be able to list recurring clusters from the CLI as JSON.
- Users must be able to inspect one cluster’s detailed occurrences and text history.
- Query bridge cluster mode must emit cluster summaries without breaking existing case, delta,
  aggregate, or signal modes.
