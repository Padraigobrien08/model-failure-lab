# 105-01 Plan

1. Formalize the Phase 105 backend contract in
   [`src/model_failure_lab/governance/policy.py`](/Users/padraigobrien/model-failure-lab/src/model_failure_lab/governance/policy.py)
   by promoting the draft escalation and lifecycle dataclasses into a stable recommendation payload
   and exposing the family-lifecycle helper seam needed by later phases.
2. Tighten lifecycle semantics so `keep`, `prune`, `merge_candidate`, and `retire` are derived
   from deterministic family history and provenance, including correcting merge-candidate matching
   and carrying the evidence needed for `ESC-02` and `HEALTH-02`.
3. Add focused governance/history tests that prove escalation bands, lifecycle health outcomes,
   and deterministic payload stability without yet expanding CLI or debugger presentation work.
