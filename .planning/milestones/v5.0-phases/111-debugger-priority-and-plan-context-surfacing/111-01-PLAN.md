# 111-01 Plan

1. Extend the dataset-versions bridge payload with optional portfolio item and saved-plan context
   for the requested family.
2. Add frontend artifact types/loaders for the new portfolio payloads while preserving backward
   compatibility with old fixtures and missing fields.
3. Surface compact priority and saved-plan context in the existing dataset automation panel on the
   analysis and comparison routes, then cover the behavior with focused route tests.
