# 115-01 Plan

1. Enrich signal-analysis query rows with portfolio priority and saved-plan context so analysis
   presets can target plan-backed and queue-aware operator workflows.
2. Add URL-backed intent presets and preset-summary surfaces to `/analysis` while preserving the
   existing raw filter controls and drillthrough routes.
3. Strengthen the shared shell with explicit workspace source, status, and orientation cues, then
   cover the behavior with focused analysis-route tests and a real signal-query bridge check.
