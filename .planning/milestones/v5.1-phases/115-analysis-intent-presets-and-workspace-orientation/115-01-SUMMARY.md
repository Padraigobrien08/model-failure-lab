# 115-01 Summary

- Added URL-backed operator presets to `/analysis` with preset-summary context and visible-row
  counts so the route can start from intent instead of a blank filter wall.
- Enriched signal-analysis rows with portfolio priority and saved-plan context from the query
  bridge, enabling plan-backed preset logic and richer signal-row surfacing.
- Strengthened the shared shell with explicit workspace source, status, path, and a skip link so
  artifact-root trust is visible across non-detail routes.
