# 117-01 Plan

1. Add dedicated storage helpers and a governance execution module for saved-plan preflight,
   checkpoint, snapshot, follow-up, and receipt artifacts.
2. Implement preflight validation that blocks stale or missing-family actions before any lifecycle
   mutation and records minimal before/after family-state snapshots.
3. Add focused governance CLI tests that prove ready, already-applied, and blocked preflight
   outcomes over real fixture artifacts.
