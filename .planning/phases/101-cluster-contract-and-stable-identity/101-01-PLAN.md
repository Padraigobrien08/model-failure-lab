# 101-01 Plan

1. Extend the derived SQLite index schema with persisted cluster occurrences and a new schema
   version.
2. Add deterministic cluster-id builders for saved run failures and comparison deltas.
3. Expose cluster list/detail helpers over the persisted occurrence table and prove stability
   against repeated index rebuilds.
