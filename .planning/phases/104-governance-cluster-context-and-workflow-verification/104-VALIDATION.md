# 104 Validation

- Governance recommendations should include recurring cluster context when the comparison belongs to
  a repeated pattern.
- History snapshots should surface recurring clusters alongside recurring failures and trends.
- The real-artifact smoke must validate cluster queries in the same configured artifact root as the
  existing overview/query/governance checks.
