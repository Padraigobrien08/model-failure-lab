# 110-01 Plan

1. Add `dataset` CLI surfaces for portfolio queue listing and inspectable planning units using the
   Phase 109 backend contract.
2. Add deterministic saved-plan creation, listing, inspection, and explicit action-promotion
   commands backed by portfolio-plan artifacts under governance storage.
3. Add focused CLI regression coverage proving JSON payloads, filtering, and plan promotion into
   the existing lifecycle apply workflow.
