# 118-01 Plan

1. Add dataset CLI commands for saved-plan preflight, execution, receipt listing, and execution
   detail inspection.
2. Render checkpoint progress, remaining family actions, rollback guidance, and prepared
   rerun/compare follow-up in both JSON and human-readable CLI output.
3. Extend governance CLI tests to cover the `plan-create -> plan-preflight -> plan-execute ->
   executions -> execution-show` workflow.
