---
phase: 37-summary-entry-route
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/summaryRoute.ts
  - frontend/src/app/routes/SummaryPage.tsx
  - frontend/src/components/summary/VerdictStrip.tsx
  - frontend/src/components/summary/LaneSummaryPanel.tsx
  - frontend/src/app/App.tsx
  - frontend/src/app/__tests__/traceRoutes.test.tsx
autonomous: true
requirements: [SUM-01]
user_setup: []
must_haves:
  truths:
    - "The `/` route stops rendering the generic placeholder and immediately shows a compact verdict strip plus two lane panels for `Robustness` and `Calibration`."
    - "The summary route stays mocked-data friendly while already using future route-aligned `laneId` and `methodId` values for later drilldown."
    - "Only the home route becomes real in Phase 37; the sticky header, hidden-inspector shell, and remaining placeholder routes stay intact."
  artifacts:
    - path: frontend/src/lib/summaryRoute.ts
      provides: "Typed summary-route snapshot and mocked lane or method preview data"
      contains: "temperature_scaling"
    - path: frontend/src/app/routes/SummaryPage.tsx
      provides: "Real home-route surface that answers `Where should I look?`"
      contains: "Robustness"
    - path: frontend/src/components/summary/VerdictStrip.tsx
      provides: "Compact final-verdict strip with status and one-sentence implication"
      contains: "implication"
  key_links:
    - from: frontend/src/app/App.tsx
      to: frontend/src/app/routes/SummaryPage.tsx
      via: "the `/` route mounts the first real summary surface instead of `VerdictPlaceholderPage`"
      pattern: "path=\"/\""
    - from: frontend/src/app/routes/SummaryPage.tsx
      to: frontend/src/lib/summaryRoute.ts
      via: "summary route reads one mocked snapshot keyed to `robustness` and `calibration`"
      pattern: "calibration"
    - from: .planning/phases/36-trace-shell-and-route-scaffold/36-CONTEXT.md
      to: frontend/src/app/routes/SummaryPage.tsx
      via: "the summary page must stay inside the existing header-only shell with no visible inspector"
      pattern: "hidden inspector"
---

<objective>
Replace only the `/` placeholder with the first real summary route built on a narrow mocked contract.

Purpose: Phase 36 locked the route shell and scope behavior, but the home route still answers nothing. Phase 37 starts the real trace chain by turning `/` into a compact verdict-plus-lanes waypoint without reopening shell, inspector, backend, or dashboard scope.
Output: one mocked summary-route model, one real `SummaryPage`, one compact `VerdictStrip`, one split `LaneSummaryPanel`, and a direct-route test update for `/`.
</objective>

<execution_context>
@/Users/padraigobrien/model-failure-lab/.codex/get-shit-done/workflows/execute-plan.md
@/Users/padraigobrien/model-failure-lab/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-summary-entry-route/37-CONTEXT.md
@.planning/phases/36-trace-shell-and-route-scaffold/36-CONTEXT.md
@.planning/phases/36-trace-shell-and-route-scaffold/36-02-SUMMARY.md
@frontend/src/app/App.tsx
@frontend/src/app/scope.tsx
@frontend/src/app/routes/VerdictPlaceholderPage.tsx
@frontend/src/components/layout/TraceHeader.tsx
@frontend/src/components/layout/TraceShell.tsx
@frontend/src/app/__tests__/traceRoutes.test.tsx
@frontend/src/lib/formatters.ts

<interfaces>
From `frontend/src/app/scope.tsx`:
```tsx
export type TraceScope = "official" | "all";

export function useTraceScope(): {
  scope: TraceScope;
  setScope: (next: TraceScope) => void;
};
```

From `frontend/src/app/App.tsx`:
```tsx
<Route path="/" element={<VerdictPlaceholderPage />} />
<Route path="/lane/:laneId" element={<LanePlaceholderPage />} />
<Route path="/lane/:laneId/:methodId" element={<MethodPlaceholderPage />} />
```

From `frontend/src/lib/formatters.ts`:
```ts
export function formatLabel(value: string | null | undefined): string;
export function formatMetric(value: number | null | undefined, digits?: number): string;
export function formatSignedMetric(value: number | null | undefined, digits?: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the mocked summary-route contract and lane preview data</name>
  <files>frontend/src/lib/summaryRoute.ts</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/37-summary-entry-route/37-CONTEXT.md, .planning/phases/36-trace-shell-and-route-scaffold/36-02-SUMMARY.md, frontend/src/app/scope.tsx, frontend/src/lib/formatters.ts</read_first>
  <action>Create `frontend/src/lib/summaryRoute.ts` as the single mocked-data contract for Phase 37. Export typed models for the compact verdict strip, the two lane panels, the lane metrics list, and the method preview rows. Use exact lane IDs `robustness` and `calibration`, and future route-aligned method IDs `baseline`, `reweighting`, `temperature_scaling`, plus one exploratory-only robustness method such as `group_dro`. The exported mock snapshot must already encode final verdict status, one-sentence implication, lane status, one-line lane summary, compact metrics, baseline-first preview rows, and per-row scope metadata (`official` vs `exploratory`). Do not import manifest loaders, `buildOverviewSnapshot`, `buildVerdictWorkspaceModel`, or any backend/data bridge; this file must stay mocked-data friendly and Phase-37-narrow.</action>
  <verify>
    <automated>rg -n "robustness|calibration|baseline|reweighting|temperature_scaling|group_dro|exploratory" frontend/src/lib/summaryRoute.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/summaryRoute.ts` contains the exact strings `robustness`, `calibration`, `baseline`, `reweighting`, `temperature_scaling`, and `group_dro`
    - `frontend/src/lib/summaryRoute.ts` contains the exact strings `official` and `exploratory`
    - `frontend/src/lib/summaryRoute.ts` exports one top-level summary snapshot or builder instead of scattering mock data across route components
    - `frontend/src/lib/summaryRoute.ts` does not import `loadArtifactIndex`, `buildOverviewSnapshot`, or `buildVerdictWorkspaceModel`
  </acceptance_criteria>
  <done>Phase 37 has one explicit mocked summary-route contract aligned to the future lane and method routes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the `/` placeholder with a compact verdict strip and split lane panels</name>
  <files>frontend/src/app/routes/SummaryPage.tsx, frontend/src/components/summary/VerdictStrip.tsx, frontend/src/components/summary/LaneSummaryPanel.tsx, frontend/src/app/App.tsx, frontend/src/app/__tests__/traceRoutes.test.tsx</files>
  <read_first>.planning/PROJECT.md, .planning/phases/37-summary-entry-route/37-CONTEXT.md, .planning/phases/36-trace-shell-and-route-scaffold/36-CONTEXT.md, frontend/src/app/App.tsx, frontend/src/app/routes/VerdictPlaceholderPage.tsx, frontend/src/components/layout/TraceHeader.tsx, frontend/src/components/layout/TraceShell.tsx, frontend/src/app/__tests__/traceRoutes.test.tsx, frontend/src/lib/summaryRoute.ts</read_first>
  <action>Create `frontend/src/app/routes/SummaryPage.tsx` and mount it only on `/` in `frontend/src/app/App.tsx`, leaving the lane, method, run, and raw placeholder routes untouched. `SummaryPage` should keep the route question `Where should I look?` compact, render one `VerdictStrip` with status, one-sentence implication, and a small current-scope label, then render exactly two lane panels labeled `Robustness` and `Calibration`. `LaneSummaryPanel` must use the locked split layout: left side for lane status, one-line summary, and compact metrics; right side for a 2-3 row method preview. Keep preview ordering baseline-first and cap it at three rows total, but leave whole-panel click behavior, inline method-link behavior, and scope-specific exploratory row hardening for Plan 37-02. Update `frontend/src/app/__tests__/traceRoutes.test.tsx` so the `/` route expects the new summary surface instead of the old placeholder params block while the other route placeholder assertions remain unchanged. Do not import `TraceRoutePlaceholder`, `OverviewPage`, `RankedComparisonCanvas`, lane tables, run detail, inspector UI, or backend selectors.</action>
  <verify>
    <automated>rg -n "SummaryPage|VerdictStrip|LaneSummaryPanel|Where should I look\\?|Robustness|Calibration" frontend/src/app/App.tsx frontend/src/app/routes/SummaryPage.tsx frontend/src/components/summary/VerdictStrip.tsx frontend/src/components/summary/LaneSummaryPanel.tsx</automated>
    <automated>npm --prefix frontend run test -- --run traceRoutes</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/App.tsx` imports `SummaryPage` and does not mount `VerdictPlaceholderPage` on `path="/"` anymore
    - `frontend/src/app/routes/SummaryPage.tsx` contains the exact text `Where should I look?`
    - `frontend/src/app/routes/SummaryPage.tsx` contains the exact lane labels `Robustness` and `Calibration`
    - `frontend/src/app/routes/SummaryPage.tsx` does not import `TraceRoutePlaceholder`, `buildOverviewSnapshot`, `buildVerdictWorkspaceModel`, or `OverviewPage`
    - `frontend/src/app/__tests__/traceRoutes.test.tsx` no longer expects `No dynamic params for this route.` for `/`
    - `npm --prefix frontend run test -- --run traceRoutes` exits 0
  </acceptance_criteria>
  <done>The home route now renders the first real summary surface inside the existing Phase 36 shell.</done>
</task>

</tasks>

<verification>
- [ ] `npm --prefix frontend run test -- --run traceRoutes` succeeds
- [ ] `/` renders a compact verdict strip plus exactly two lane panels labeled `Robustness` and `Calibration`
- [ ] the summary route uses mocked route-aligned data rather than manifest or backend selectors
- [ ] the sticky header, hidden-inspector shell, and non-home placeholder routes remain intact
</verification>

<success_criteria>
- All tasks completed
- `SUM-01` gains the first real route surface for `/` without widening into lane tables, run detail, or dashboard chrome
- Phase 37 is ready for interaction hardening and focused summary-route tests in Plan 37-02
</success_criteria>

<output>
After completion, create `.planning/phases/37-summary-entry-route/37-01-SUMMARY.md`
</output>
