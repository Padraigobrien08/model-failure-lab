---
phase: 37-summary-entry-route
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - frontend/src/lib/summaryRoute.ts
  - frontend/src/app/routes/SummaryPage.tsx
  - frontend/src/components/summary/LaneSummaryPanel.tsx
  - frontend/src/app/__tests__/summaryRoute.test.tsx
autonomous: true
requirements: [SUM-01]
user_setup: []
must_haves:
  truths:
    - "Each lane panel is a real drilldown surface: clicking the panel opens the lane route, while clicking a method name skips directly to the method route."
    - "Baseline stays first in every preview, and exploratory rows appear only when scope is `all`, marked with row-level `Exploratory` badges instead of lane-level warnings."
    - "Focused summary-route tests pin the scope-aware preview, the whole-lane click, and the inline method-link behavior without changing the existing header-only shell."
  artifacts:
    - path: frontend/src/components/summary/LaneSummaryPanel.tsx
      provides: "Clickable split lane panel with inline method links"
      contains: "Exploratory"
    - path: frontend/src/lib/summaryRoute.ts
      provides: "Scope-aware summary preview builder that keeps baseline first"
      contains: "baseline"
    - path: frontend/src/app/__tests__/summaryRoute.test.tsx
      provides: "Focused regression coverage for Phase 37 summary route behavior"
      contains: "scope=all"
  key_links:
    - from: frontend/src/components/summary/LaneSummaryPanel.tsx
      to: frontend/src/app/scope.tsx
      via: "lane and method drilldowns must preserve `?scope=official|all`"
      pattern: "scope=all"
    - from: frontend/src/lib/summaryRoute.ts
      to: frontend/src/components/summary/LaneSummaryPanel.tsx
      via: "scope-aware preview rows determine baseline ordering and exploratory badge visibility"
      pattern: "exploratory"
    - from: frontend/src/app/__tests__/summaryRoute.test.tsx
      to: frontend/src/app/App.tsx
      via: "MemoryRouter route tests verify whole-panel and inline-method drilldown behavior from `/`"
      pattern: "/lane/robustness"
---

<objective>
Harden the summary route into a real entrypoint with lane and method drilldowns plus focused regression coverage.

Purpose: Plan 37-01 replaces the placeholder with a real route surface, but the home screen still needs the final behavior contract locked: scope-aware preview rows, row-level exploratory badges, whole-panel navigation, and method-level shortcut links.
Output: one scope-aware summary preview builder, one fully interactive `LaneSummaryPanel`, and one focused `summaryRoute.test.tsx` suite.
</objective>

<execution_context>
@/Users/padraigobrien/model-failure-lab/.codex/get-shit-done/workflows/execute-plan.md
@/Users/padraigobrien/model-failure-lab/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-summary-entry-route/37-CONTEXT.md
@.planning/phases/37-summary-entry-route/37-01-PLAN.md
@.planning/phases/36-trace-shell-and-route-scaffold/36-02-SUMMARY.md
@frontend/src/app/scope.tsx
@frontend/src/app/routes/SummaryPage.tsx
@frontend/src/components/summary/LaneSummaryPanel.tsx
@frontend/src/lib/summaryRoute.ts
@frontend/src/app/__tests__/traceRoutes.test.tsx
@frontend/package.json

<interfaces>
Plan `37-01` creates the following contract and this plan should preserve it:
```ts
export type SummaryLaneId = "robustness" | "calibration";

export type SummaryMethodPreviewRow = {
  methodId: string;
  label: string;
  scope: "official" | "exploratory";
  status: string;
};

export type SummaryLaneModel = {
  laneId: SummaryLaneId;
  label: string;
  status: string;
  summary: string;
  metrics: Array<{ label: string; value: string }>;
  previewRows: SummaryMethodPreviewRow[];
};

export function buildSummaryRouteModel(scope: "official" | "all"): {
  verdict: { status: string; implication: string };
  lanes: SummaryLaneModel[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden lane-panel drilldown behavior and scope-aware preview rows</name>
  <files>frontend/src/lib/summaryRoute.ts, frontend/src/app/routes/SummaryPage.tsx, frontend/src/components/summary/LaneSummaryPanel.tsx</files>
  <read_first>.planning/PROJECT.md, .planning/phases/37-summary-entry-route/37-CONTEXT.md, .planning/phases/37-summary-entry-route/37-01-PLAN.md, frontend/src/app/scope.tsx, frontend/src/app/routes/SummaryPage.tsx, frontend/src/components/summary/LaneSummaryPanel.tsx, frontend/src/lib/summaryRoute.ts</read_first>
  <action>Finish the home-route drilldown behavior without widening scope. In `frontend/src/lib/summaryRoute.ts`, expose a scope-aware builder that returns only official preview rows for `scope="official"` and adds exploratory rows only when `scope="all"`, while always keeping `baseline` as preview row 1 and capping each lane to 2-3 rows total. In `frontend/src/components/summary/LaneSummaryPanel.tsx`, make the entire panel navigate to `/lane/:laneId?scope={scope}` as the primary affordance and render each preview method name as an inline link to `/lane/:laneId/:methodId?scope={scope}`. Ensure method-link clicks do not trigger the panel-level navigation, and render the exact row badge text `Exploratory` only on preview rows whose scope is exploratory. Keep the split panel shape from Plan 37-01, do not add `Open lane` buttons, lane-level warning banners, lane tables, run detail, or inspector UI, and keep the `SummaryPage` layout limited to the verdict strip plus the two lane panels.</action>
  <verify>
    <automated>rg -n "Exploratory|baseline|official|scope === \\\"all\\\"|stopPropagation|preventDefault|/lane/" frontend/src/lib/summaryRoute.ts frontend/src/components/summary/LaneSummaryPanel.tsx frontend/src/app/routes/SummaryPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/summary/LaneSummaryPanel.tsx` constructs both lane links and method links under `/lane/`
    - `frontend/src/components/summary/LaneSummaryPanel.tsx` contains the exact text `Exploratory`
    - `frontend/src/lib/summaryRoute.ts` contains the exact strings `official`, `all`, and `baseline`
    - `frontend/src/lib/summaryRoute.ts` limits preview rows to at most three per lane while keeping `baseline` present
    - none of the files in this task contain `Open lane`, `Open method`, `Run detail`, or `TraceRoutePlaceholder`
    - `frontend/src/app/routes/SummaryPage.tsx` still renders exactly two lane panels
  </acceptance_criteria>
  <done>The summary panels now behave as real drilldown surfaces with scope-aware preview rows and row-level exploratory labeling.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused summary-route regression coverage and rerun frontend verification</name>
  <files>frontend/src/app/__tests__/summaryRoute.test.tsx</files>
  <read_first>.planning/phases/37-summary-entry-route/37-CONTEXT.md, .planning/phases/37-summary-entry-route/37-01-PLAN.md, frontend/package.json, frontend/src/app/App.tsx, frontend/src/app/routes/SummaryPage.tsx, frontend/src/components/summary/LaneSummaryPanel.tsx, frontend/src/lib/summaryRoute.ts, frontend/src/app/__tests__/traceRoutes.test.tsx</read_first>
  <action>Add `frontend/src/app/__tests__/summaryRoute.test.tsx` with focused RTL coverage for the Phase 37 home route. Mount `App` with `useMemoryRouter` at `/` and `/?scope=all`. Assert the verdict strip renders, the exact lane labels `Robustness` and `Calibration` are present, baseline rows appear before ranked methods, no `Exploratory` badge appears in official scope, `Exploratory` appears on the expected row in `scope=all`, clicking the robustness panel navigates to `/lane/robustness?scope=all`, and clicking the inline `reweighting` method link navigates to `/lane/robustness/reweighting?scope=all` without falling back to the whole-panel target. Keep `frontend/src/app/__tests__/traceRoutes.test.tsx` and `frontend/src/app/__tests__/shell.test.tsx` intact unless a tiny assertion adjustment is required for the new home-route contract.</action>
  <verify>
    <automated>npm --prefix frontend run test -- --run summaryRoute traceRoutes shell</automated>
    <automated>MPLCONFIGDIR=/tmp/matplotlib npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/__tests__/summaryRoute.test.tsx` contains the exact strings `Robustness`, `Calibration`, `Exploratory`, `/lane/robustness?scope=all`, and `/lane/robustness/reweighting?scope=all`
    - `frontend/src/app/__tests__/summaryRoute.test.tsx` asserts baseline preview ordering in at least one lane
    - `frontend/src/app/__tests__/summaryRoute.test.tsx` covers both whole-panel click and inline method-link click behavior
    - `npm --prefix frontend run test -- --run summaryRoute traceRoutes shell` exits 0
    - `MPLCONFIGDIR=/tmp/matplotlib npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>The Phase 37 summary route is pinned by focused tests and still passes targeted frontend verification.</done>
</task>

</tasks>

<verification>
- [ ] `npm --prefix frontend run test -- --run summaryRoute traceRoutes shell` succeeds
- [ ] `MPLCONFIGDIR=/tmp/matplotlib npm --prefix frontend run build` succeeds
- [ ] whole-lane clicks open `/lane/:laneId` while inline method links open `/lane/:laneId/:methodId`
- [ ] exploratory badges appear only at the row level when `scope=all`
- [ ] the summary route still preserves the existing sticky header and hidden-inspector shell
</verification>

<success_criteria>
- All tasks completed
- `SUM-01` is fully covered: user lands on `/`, reads the verdict and lane panels, and can drill toward lane or method detail immediately
- Phase 37 is ready for Phase 38 to replace the lane placeholder with the table-first lane workspace
</success_criteria>

<output>
After completion, create `.planning/phases/37-summary-entry-route/37-02-SUMMARY.md`
</output>
