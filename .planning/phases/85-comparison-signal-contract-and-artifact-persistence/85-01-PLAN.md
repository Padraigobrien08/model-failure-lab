# 85-01 Plan

- Add a deterministic comparison signal builder over failure-rate deltas and saved case-delta rows.
- Persist the signal block into both summary and detail comparison artifacts.
- Extend comparison reporting tests to cover improvement, regression, and incompatible signal cases.

