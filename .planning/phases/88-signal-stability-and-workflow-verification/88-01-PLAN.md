# 88-01 Plan

1. Add a CLI workflow stability test that repeats the same comparison, rebuilds the query index,
   and proves the persisted signal contract stays unchanged.
2. Extend the real-artifacts smoke script to verify comparison signal presence and the `signals`
   analysis query path with the existing latency budget.
3. Run the milestone verification bundle across backend signal tests, CLI signal tests, debugger
   route tests, the frontend build, and the smoke script, then close the milestone state.
