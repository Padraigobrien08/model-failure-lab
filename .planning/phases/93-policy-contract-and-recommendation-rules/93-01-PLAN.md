# 93-01 Plan

1. Refactor dataset evolution to expose a dry-run regression-pack preview that reuses the existing
   deterministic signal and case-selection logic without writing dataset files.
2. Add a governance policy/recommendation module that evaluates one saved comparison into
   `create`, `evolve`, or `ignore`.
3. Include explicit rationale, policy snapshot, matched-family metadata, and evidence preview in
   the recommendation payload.
