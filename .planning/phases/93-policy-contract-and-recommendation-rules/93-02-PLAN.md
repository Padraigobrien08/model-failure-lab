# 93-02 Plan

1. Add unit coverage for recommendation determinism, severity thresholds, failure-type filters, and
   exact-family `create` versus `evolve` behavior.
2. Add family-cap and duplicate-growth guard coverage so policy inputs are real and inspectable,
   not placeholder fields.
3. Export the new governance backend surface for later CLI and debugger phases without adding user
   commands yet.
