# Quick Task 260326-o2w — Trace Pill Navigation

**Date:** 2026-03-26  
**Description:** I need a way to move from verdict to lane and further etc, make the pills clickable and maybe add arrows

## Goal

Turn the trace pills in the sticky header into real, scope-preserving navigation so users can move backward through the current trace without relying only on page-local links.

## Tasks

1. Add contextual trace-chain links in the header.
   - Update the header trace pills to render clickable links when the current route provides enough context.
   - Keep the active step highlighted and keep unavailable future steps visibly disabled.
   - Replace the plain text separators with directional arrows.

2. Add focused regression coverage.
   - Prove the header trace chain exposes the right scoped links on the method route.
   - Prove clicking the trace pills navigates backward without losing `?scope=all`.
   - Tighten any placeholder-route tests that become ambiguous because the header now contains matching links.

## Files

- `frontend/src/components/layout/TraceHeader.tsx`
- `frontend/src/app/__tests__/shell.test.tsx`
- `frontend/src/app/__tests__/traceRoutes.test.tsx`

## Verification

- `npm --prefix frontend run test -- --run shell traceRoutes`
- `MPLCONFIGDIR=/tmp/matplotlib npm --prefix frontend run build`
