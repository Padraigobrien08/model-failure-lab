# Product Screens

> No screenshots are included yet. The image files previously referenced here
> (`docs/screens/*.png`, `*.gif`) do not exist, so the embeds were removed to avoid broken images.
> Add real captures under `docs/screens/` and replace the placeholders below with `![...](screens/…)`
> embeds when available. (Adding screenshots is tracked as a P1 item in `docs/oss-readiness.md`.)

## Run summary

_Screenshot placeholder — capture `failure-lab run` output and save as `docs/screens/run-summary.png`._

## Failure inventory

_Screenshot placeholder — capture the failure inventory view and save as
`docs/screens/failure-inventory.png`._

## Comparison view

_Screenshot placeholder — capture `failure-lab compare` output and save as
`docs/screens/comparison-view.png`._

## Harvest / replay workflow

_Screenshot/GIF placeholder — capture the harvest → promote → rerun loop and save as
`docs/screens/harvest-replay-workflow.gif`._
